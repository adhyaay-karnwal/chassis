name: Benchmarks

on:
  workflow_dispatch:
    inputs:
      signed_run:
        description: Successful main Release validation run to compare (no signing credentials used)
        type: string
        default: ""
  push:
    branches: [main]
  pull_request:

concurrency:
  group: bench-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  bench:
    if: inputs.signed_run == ''
    name: Startup Latency
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Zig
        uses: mlugg/setup-zig@v2
        with:
          version: "0.16.0"

      - name: Build (ReleaseSafe)
        run: zig build -Doptimize=ReleaseSafe

      - name: Install hyperfine
        run: |
          wget -q https://github.com/sharkdp/hyperfine/releases/download/v1.19.0/hyperfine_1.19.0_amd64.deb
          sudo dpkg -i hyperfine_1.19.0_amd64.deb

      - name: Run benchmarks
        run: ./benchmarks/startup.sh --ci

      - name: Enforce latency budgets
        run: python3 benchmarks/check_budgets.py

      - name: Run UI activity progress benchmark
        run: |
          for _ in 1 2 3; do
            zig build run-bench-ui-activity -Doptimize=ReleaseSafe
          done

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2

      - name: Run interactive terminal performance gate
        working-directory: tests/e2e
        env:
          CHASSIS_TUI_PERFORMANCE: "1"
          CHASSIS_TUI_PERFORMANCE_REPORT: ${{ runner.temp }}/tui-performance.json
        run: bun test tui-performance.test.ts

      - name: Upload interactive terminal performance evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: tui-performance
          path: ${{ runner.temp }}/tui-performance.json
          if-no-files-found: warn

  libchassis-ttft:
    if: inputs.signed_run == ''
    name: libchassis Runtime
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Zig
        uses: mlugg/setup-zig@v2
        with:
          version: "0.16.0"

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "24"

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: "1.4.2"

      - name: Build libchassis artifacts
        run: |
          zig build -Dnapi-surface=core -Doptimize=ReleaseSafe
          zig build -Dwasm-surface=core -Doptimize=ReleaseSmall

      - name: Install pinned Pi benchmark target
        run: npm install --prefix /tmp/libchassis-pi @earendil-works/pi-coding-agent@0.84.4

      - name: Build external inference fixture
        run: zig build-exe benchmarks/libchassis/fake-inference-server.zig -O ReleaseSafe -femit-bin=${{ runner.temp }}/libchassis-fake-inference-server

      - name: Measure libchassis and Pi
        run: |
          node benchmarks/libchassis/bench-suite.mjs --samples 100 --pi-root /tmp/libchassis-pi --out benchmarks/results/libchassis
          node benchmarks/libchassis/bench-bridge.mjs --backend native --samples 100 > benchmarks/results/libchassis/bridge-node-native.json
          node --experimental-wasm-jspi benchmarks/libchassis/bench-bridge.mjs --backend wasm --samples 100 > benchmarks/results/libchassis/bridge-node-wasm.json
          bun benchmarks/libchassis/bench-bridge.mjs --backend native --samples 100 > benchmarks/results/libchassis/bridge-bun-native.json
          bun benchmarks/libchassis/bench-bridge.mjs --backend wasm --samples 100 > benchmarks/results/libchassis/bridge-bun-wasm.json
          node benchmarks/libchassis/bench-runtime.mjs --backend native --warm-samples 100 --stream-samples 30 > benchmarks/results/libchassis/runtime-node-native.json
          node --experimental-wasm-jspi benchmarks/libchassis/bench-runtime.mjs --backend wasm --warm-samples 100 --stream-samples 30 > benchmarks/results/libchassis/runtime-node-wasm.json
          bun benchmarks/libchassis/bench-runtime.mjs --backend native --warm-samples 100 --stream-samples 30 > benchmarks/results/libchassis/runtime-bun-native.json
          bun benchmarks/libchassis/bench-runtime.mjs --backend wasm --warm-samples 100 --stream-samples 30 > benchmarks/results/libchassis/runtime-bun-wasm.json
          node --expose-gc benchmarks/libchassis/bench-capacity.mjs --backend native --count 25 > benchmarks/results/libchassis/capacity-node-native.json
          node --experimental-wasm-jspi --expose-gc benchmarks/libchassis/bench-capacity.mjs --backend wasm --count 25 > benchmarks/results/libchassis/capacity-node-wasm.json
          bun benchmarks/libchassis/bench-capacity.mjs --backend native --count 25 > benchmarks/results/libchassis/capacity-bun-native.json
          bun benchmarks/libchassis/bench-capacity.mjs --backend wasm --count 25 > benchmarks/results/libchassis/capacity-bun-wasm.json
          node benchmarks/libchassis/bench-competitive.mjs --server ${{ runner.temp }}/libchassis-fake-inference-server --pi-root /tmp/libchassis-pi --out benchmarks/results/libchassis --samples 100 --warmups 3 --rounds 3

      - name: Enforce deterministic libchassis contracts
        run: node benchmarks/libchassis/check-results.mjs --dir benchmarks/results/libchassis

      - name: Upload raw libchassis TTFT samples
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: libchassis-ttft-${{ github.sha }}
          path: benchmarks/results/libchassis/*.json
          if-no-files-found: error

  signed-macos:
    name: Signed macOS comparison
    if: github.event_name == 'workflow_dispatch' && inputs.signed_run != ''
    runs-on: macos-15
    timeout-minutes: 20
    permissions:
      actions: read
      contents: read
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      - name: Download a successful main validation pair
        env:
          GH_TOKEN: ${{ github.token }}
          SIGNED_RUN: ${{ inputs.signed_run }}
        run: |
          [[ "$SIGNED_RUN" =~ ^[1-9][0-9]*$ ]]
          gh api "repos/$GITHUB_REPOSITORY/actions/runs/$SIGNED_RUN" > "$RUNNER_TEMP/signed-source.json"
          jq -e --arg repo "$GITHUB_REPOSITORY" '
            .conclusion == "success" and .head_branch == "main" and
            .event == "workflow_dispatch" and .path == ".github/workflows/release.yml" and
            .head_repository.full_name == $repo
          ' "$RUNNER_TEMP/signed-source.json"
          gh run download "$SIGNED_RUN" --repo "$GITHUB_REPOSITORY" \
            --name chassis-macos-aarch64 --name signing-control-macos-arm64 \
            --dir "$RUNNER_TEMP/signed-input"
          mkdir -p "$RUNNER_TEMP/signed-control" "$RUNNER_TEMP/signed-candidate"
          tar -xzf "$RUNNER_TEMP/signed-input/signing-control-macos-arm64/chassis-signing-control.tar.gz" -C "$RUNNER_TEMP/signed-control" chassis
          tar -xzf "$RUNNER_TEMP/signed-input/chassis-macos-aarch64/chassis-macos-aarch64.tar.gz" -C "$RUNNER_TEMP/signed-candidate" chassis

      - name: Install measurement tools
        env:
          HOMEBREW_NO_AUTO_UPDATE: "1"
        run: |
          command -v hyperfine >/dev/null || brew install hyperfine
          xcrun clang -O2 benchmarks/signed_macos_resources.c -o "$RUNNER_TEMP/signed-resources"

      - uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2
        with:
          bun-version: "1.3.14"

      - name: Compare signed startup
        run: |
          python3 -m scripts.compare_signed_macos \
            --control "$RUNNER_TEMP/signed-control/chassis" \
            --candidate "$RUNNER_TEMP/signed-candidate/chassis" \
            --source-sha "$(jq -r .head_sha "$RUNNER_TEMP/signed-source.json")" \
            --output "$RUNNER_TEMP/signed-comparison"

      - name: Compare native image-flow memory
        env:
          CHASSIS_SIGNED_COMPARE_DIR: ${{ runner.temp }}/signed-comparison
          CHASSIS_SIGNED_RESOURCE_PROBE: ${{ runner.temp }}/signed-resources
        run: bun test benchmarks/signed_macos_memory.test.ts

      - name: Retain paired measurements
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: signed-macos-comparison-${{ github.run_id }}
          path: |
            ${{ runner.temp }}/signed-comparison/manifest.json
            ${{ runner.temp }}/signed-comparison/startup-*.json
            ${{ runner.temp }}/signed-comparison/startup-*/logs/
            ${{ runner.temp }}/signed-comparison/memory-*.json
            ${{ runner.temp }}/signed-source.json
          if-no-files-found: error
